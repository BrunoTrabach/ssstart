name: Build SSStart

on:
  push:
    branches: [ main, v2-dev ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar SDK 34
        run: |
          sdkmanager --install "platforms;android-34" "build-tools;34.0.0"
          yes | sdkmanager --licenses

      - name: Build com Gradle 8.7
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.7
          arguments: assembleDebug --stacktrace

      - name: Copiar APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/*.apk release/SSStart-v2-dev.apk

      - name: Instala rclone
        run: sudo apt-get update && sudo apt-get install -y rclone jq

      - name: Configura rclone
        run: |
          mkdir -p ~/.config/rclone
          echo "${{ secrets.RCLONE_CONF_B64 }}" | base64 -d > ~/.config/rclone/rclone.conf

      - name: Sobe APK pro Drive
        run: rclone copy release/SSStart-v2-dev.apk gdrive-synapseshade:SSStart-TV-Updates/v2-dev/ -P

      - name: Atualiza version.json
        run: |
          rclone copy gdrive-synapseshade:SSStart-TV-Updates/v2-dev/version.json . || echo '{"versionCode":5}' > version.json
          CODE=$(jq .versionCode version.json)
          NEW=$((CODE+1))
          jq --argjson n $NEW '.versionCode=$n | .versionName="2.0-dev.\($n)" | .changelog="Auto build \($n) - '"$(date +%d/%m/%Y)"'"' version.json > v.json
          mv v.json version.json
          cat version.json
          rclone copy version.json gdrive-synapseshade:SSStart-TV-Updates/v2-dev/ -P

      - name: Commit APK
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "ci: APK v2-dev build ${{ github.run_number }}"
          file_pattern: release/*.apk
